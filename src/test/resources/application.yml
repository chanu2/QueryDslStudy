spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:tcp://localhost/~/qdsl1
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
